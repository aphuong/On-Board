
<!-- <div class="announcement-container">
  <% @announcements.last(1).each do |announcement| %>
    <h3 class="announcements"><%= announcement.body %></h3>
  <% end %>

  <h3 class="hex-title">September 2014 WDI Students:</h3>
</div> -->


<div class="index-wrapper">

  <section class="hex-section">
    <div>
      <% @hex_group_1.each do |user| %>
        <div class="hex-container-1">
          <span class="text"><%= link_to user.fname, show_user_path(user) %></span>
          <%= render 'users/hex' %>
        </div>
      <% end %>
    </div>
  </section>

  <section class="hex-section">
    <div>
      <% @hex_group_2.each do |user| %>
        <div class="hex-container-2">
          <span class="text"><%= link_to user.fname, show_user_path(user) %></span>
          <%= render 'users/hex' %>
        </div>
      <% end %>
    </div>
  </section>

  <section class="hex-section">
    <div>
      <% @hex_group_3.each do |user| %>
        <div class="hex-container-3">
          <span class="text"><%= link_to user.fname, show_user_path(user) %></span>
          <%= render 'users/hex' %>
        </div>
      <% end %>
    </div>
  </section>
  
  
</div>


